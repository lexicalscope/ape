VERSION
	procedure G(x,y) {
	   s := new();
	   call F(x);
	   t := new();
	   y.v := t;
	}
	
	procedure F(x)
		modifies x;
	{ }
VERSION  
	procedure G(x,y) {
	   s := new();
	   call F(x);
	   t := new();
	   y.v := s;
	}
	
	procedure F(x)
		modifies x;
	{ }
